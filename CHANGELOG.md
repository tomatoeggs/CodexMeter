# 更新日志

## 未发布

## v3.9.1

- 调整 `WALL-E` 主题电池状态：移除运行时绿色 / 红色电池描边，始终保留静态背景中的黑色外框。
- 充电状态改为电量百分比后的紧凑加号，并将数值与标识作为一组在电池框内水平、垂直居中；低电量仍通过红色电量文字提示。
- 完成固件编译、CodexMeter-F46690 真机烧录和 USB 截图校准，确认加号尺寸、间距与对齐符合 480×480 实屏显示。

## v3.9.0

- 为设备端主循环接入 ESP-IDF task watchdog：Arduino `loopTask` 超过 30 秒未喂狗时由系统自动复位，避免 BLE、串口和 UI tick 同时失活后只能物理重启。
- 增加基于最近有效主机 payload 的本地息屏兜底：即使 BLE callback 未正常进入断开状态，设备 5 分钟没有收到主机数据时也会记录 `host_stale` 并关闭屏幕。
- 新增串口 `health` 诊断命令，输出屏幕、BLE、最近主机 payload、最近 usage 和 watchdog 状态，便于后续区分主机未产生数据、BLE 未送达和设备主循环异常。
- 完成固件编译、宿主回归测试和 646355 真机 LittleFS / 固件烧录验证；确认 watchdog 启用、BLE 队列清零、任务状态下发和自动主题设置保持开启。

## v3.8.0

- 新增第九套设备端主题 `GARGANTUA`（内部 ID 为 `gargantua`）：以倾斜的卡冈图雅黑洞、完整的左侧吸积盘和电影式引力透镜柔光重组 480×480 Token 仪表盘。
- 使用无动态文字的 RGB565 场景作为静态层，Teko SemiBold 与 D-DIN Condensed Bold 实时绘制今日 / 近 7 天 Token、7 天剩余额度、电池、RESET 和任务数；继续复用统一 `DashboardViewModel`，不修改 macOS 程序或 BLE 协议。
- 针对 4cm 实屏完成信息层级与像素校准：顶部用量模块增加安全留白，左侧三组标签和数值统一视觉左边缘，RESET 与任务状态在右下紧凑右对齐，并为 Token 单位、三位数额度、两位数任务和长重置周期提供动态布局与上限保护。
- 保留基础黑洞原图、最终设计稿、无字运行时源图、可编辑 SVG 构图源文件与最终真机截图；完成宿主回归测试、固件编译、LittleFS / 固件烧录、自动旋转和真机可读性验证。

## v3.7.1

- 为 `d7_early_reset_jump` 增加退烧确认机制：短暂服务端空桶仍会被拦截，但同一个早期 7d 新窗口持续返回至少 3 次且满 10 分钟后会接受服务端数据。
- 按 reset horizon 识别滚动式新窗口，兼容服务端返回 `now + 7d` 的真实 reset、福利额度或策略修正，同时保留原有 7d 误显示 `100%` 的保护。
- 补充稳定器回归测试，覆盖退烧前继续拒绝和退烧后确认接受两条路径。

## v3.7.0

- 调整设备设置页顺序为主题、自动换主题、自动换主题间隔、屏幕亮度、音量和退出设置，并统一自动主题相关文案。
- 为左右 GPIO 按键增加带消抖的短按 / 长按状态机；仪表盘亮屏时长按左键切换并保存上一个主题，长按右键切换并保存下一个主题，同时避免长按误触亮度调整。
- 同步 README、架构和按键语义文档；完成宿主回归测试、固件编译、LittleFS 与固件真机烧录验证。

## v3.6.1

- 修复 `WALL-E BLUEPRINT` 主题动态文字擦除区与蓝图背景存在细微色差、真机上可见矩形遮罩的问题。
- 资源生成器改为识别连通的深海军蓝图纸区域，重建一张连续底色，同时保留暖金线稿、WALL-E 结构图与封闭阴影；加入确定性轻微抖动以降低 RGB565 色带。
- 更新干净运行时背景、RGB565 主题资源、最终真机截图和设计对比图；完成 LittleFS 烧录、串口日志验证和宿主回归测试。

## v3.6.0

- 新增第八套设备端主题 `WALL-E BLUEPRINT`（内部 ID 为 `walle_blueprint`）：以深海军蓝工程图、暖金线稿、WALL-E 双目与机身爆炸结构、机械臂、幼苗和三角履带重组完整的 480×480 Token 仪表盘。
- 使用确定性生成的无动态文字 RGB565 工程图作为静态层，Teko SemiBold 与 D-DIN Condensed Bold 实时绘制 Token、额度、电量、RESET、任务数和设备状态；继续复用统一 `DashboardViewModel`，不修改 macOS 程序或 BLE 协议。
- 新增七格动态电池与十格动态额度条；满电时七格全部点亮，额度条按剩余百分比向下取整，并统一为 18×22px 格位和 3px 间距，消除旧设计采样造成的粗细与间距差异。
- 完成 7 DAYS 数值/单位、RESET 天/小时单位的实际可见像素基线校准，简化 RESET 爆炸结构，并重新分配底栏宽度：任务区 218px、BLE 104px、SYNC 116px，三组标签使用相同字体与字号。
- 补充最终设计稿、干净运行时背景、资源生成器、真机截图、百分比与两位数任务边界验证及主题架构文档；完成宿主回归测试、固件编译、LittleFS / 固件烧录和设备日志验证。

## v3.5.0

- 新增第七套设备端主题 `WALL-E EARTH`（内部 ID 保持为 `walle_v10`）：以夕阳废土城市、幼苗靴、WALL-E 主体、磁带 RESET 面板和履带状态栏重组完整的 480×480 Token 仪表盘。
- 使用确定性生成的无动态文字 RGB565 场景承载插画与机械纹理，Jersey 10 像素字体实时绘制 Token、额度、电量、重置窗口和任务数；继续复用统一 `DashboardViewModel`，不修改 macOS 程序或 BLE 协议。
- 新增十枚叶片的亮态、灰态和掩码资源；按 7d 剩余百分比向下取整点亮，90%–99% 保持九亮一灰，100% 才全部点亮，并对十个叶槽的亮暗色阶做归一化。
- 完成顶部标题与电池、7 DAYS 数值和单位基线、`x=399` 整体中心、RESET 标签、额度百分比及 `ACTIVE TASKS` 状态栏的真机像素校准。
- 补充 Jersey 10 字体及 SIL Open Font License、可重复执行的主题资源生成器、最终设计稿、干净运行时背景、真机截图和主题架构文档；完成宿主回归测试、固件编译、LittleFS / 固件烧录与设备日志验证。

## v3.4.1

- 修复 Gundam 主题 7 DAYS 数值与单位未按字体基线对齐的问题：结合各自字体度量和纵向缩放自动计算单位位置。
- 在读取 LVGL 对象坐标前完成布局刷新，避免单位首次渲染到顶部标题区；真机验证数值与单位可见底边均为 Y=299。

## v3.4.0

- 新增 `WALL-E` 主题：将 480×480 屏幕重构为黄黑工程机器人仪表，以双目镜头、警示带、履带、磁带计时器和幼苗状态区承载完整的 Token、额度、电量、重置窗口、任务与同步信息。
- 使用最终 V9 设计稿生成无动态文字的 RGB565 机械背景，并以独立叶片纹理和掩码实现九片额度叶的实时点亮；提供可重复执行的资产生成脚本、干净源图和字体/资源说明。
- 数值与单位采用 Gundam 主题同系的 Teko SemiBold，仪表标签采用 D-DIN Condensed Bold；完成电池边框、数值基线、RESET 单位、额度居中及底部 BLE / SYNC 状态栏的真机像素校准。
- 将近 7 天 Token 达到 B/T 量级后保留两位小数的规则上移到统一 `DashboardViewModel`，所有主题共享相同格式，高达主题不再维护重复逻辑。
- 不修改 macOS 数据协议；完成固件构建、LittleFS 与固件烧录、USB 截图和设备日志验证，并补充最终设计稿、真机截图和 `1.14B` 边界截图。

## v3.3.1

- 将 Gundam 主题运行时背景替换为最终选定的 White Base / RX-78-2 清晰原稿，重新生成 480×480 RGB565 资源，改善真机背景发脏的问题。
- 当 7 DAYS 用量达到 1B 后保留两位小数（如 `1.03B`），放大单位并保持数值排版对齐；该格式仅作用于 Gundam 主题。
- 微调底部任务状态灯、任务数量、BLE 与 SYNC 的位置，更新最终真机截图，并完成固件构建、LittleFS 与设备验证。

## v3.3.0

- 新增 `Gundam` 元祖高达主题：以白色基地机库终端、RX-78-2 半身、军用遥测网格和机体状态灯重组 480×480 仪表盘；复用统一 `DashboardViewModel`，不修改 macOS 程序或 BLE 协议。
- 使用无字 RGB565 背景承载机体插画和终端纹理，LVGL 实时绘制 Token、额度、电量、重置窗口、BLE / SYNC 和任务状态；新增 D-DIN Condensed Bold 与 Teko SemiBold 字体及完整 SIL Open Font License 说明。
- 完整适配 Token 与 5h/7d 配额模式、10 段额度条和最多 7 颗任务状态灯；针对 `100%`、长短 Token 数值、空闲与多任务状态实现自适应单位布局和固定左基线。
- 完成设计稿 / 真机反复对比，校准字体、间距、装饰线、电池、传感器、状态栏和呼吸速度，并通过固件编译、真机烧录、USB 截图与宿主回归测试。

## v3.2.1

- 针对当前 Waveshare AMOLED 实屏偏艳的问题，将 `Animal Crossing` 动森主题的静态场景校准为 70% 饱和度、94% 对比度，并同步收敛动态文字、进度、状态和重置窗口配色。
- 保留未经校色的原始无字背景，新增语义化的 70% 运行时源图、最终真机截图和原图 / 校色图对比资料；补充主题资源来源、生成参数与回退说明。
- 不修改主题布局、macOS 程序或 BLE 协议；完成 RGB565 资源校验、固件编译、真机烧录与完整回归测试。

## v3.2.0

- 新增 `Animal Crossing` 动森岛屿主题：以黄色帐篷、悬挂便笺、叶片额度徽章、木制重置路标、狸克和游客状态卡重组完整仪表盘信息层级。
- 使用无字 RGB565 静态场景与透明 LVGL 动态覆盖层组合，在保留插画质感的同时实时显示 Token、额度、电量、重置窗口、同步状态和任务数量；继续复用统一 `DashboardViewModel`，不修改 macOS 程序或 BLE 协议。
- 完整适配 Token 与 5h/7d 配额模式；为 Token 单位、额度百分号和重置窗口拆分独立字号与自适应布局，并覆盖 `100%`、最多 6 个任务、空闲状态和不同数字长度的 480×480 真机验收。
- 新增 Fredoka SemiBold Latin 字体及 SIL Open Font License 声明，并补充主题背景资源、设计稿、最终实机截图和架构说明。

## v3.1.0

- 新增 `Famicom` 红白机主题：使用独立 LVGL 对象树还原象牙白机身、酒红卡带区、黑褐数据窗、SELECT / START 重置键和十字键 / A/B 控制器状态轨。
- 完整适配 Token 与配额模式、动态电量、数据状态、自动换肤和数字化运行任务计数；优化大小数字、单位、标签、重置窗口、电池组件和配额进度条在 480×480 真机上的字号、间距与对齐。
- 完成设计稿与真机截图对比，并额外验证 100% 剩余额度和 6 个运行任务的边界排版；周期数据刷新不会重复失效未变化的进度格。

## v3.0.0

- 新增 ESP32 端多主题渲染架构：以 `ThemePack`、注册表、运行时生命周期和统一 `DashboardViewModel` 隔离业务状态与 LVGL 展现；主题切换不修改 macOS 程序或 BLE 协议。
- 首发 `Classic` 与高还原度 `Cyberpunk` 两套主题；Cyberpunk 主题完整覆盖 Token 用量、7d 额度、重置窗口、电池、连接状态和最多 6 个运行任务指示，并针对 100% 与不同位数做自适应排版。
- 新增设备设置页：中间键短按进入或确认，左/右键浏览与调值；支持主题、屏幕亮度、预留音量、自动换肤开关、1–1440 分钟切换间隔和退出设置。
- 中间键亮屏/关屏改为长按；设置页、任务提醒、关屏和系统浮层期间会暂停自动换肤，返回可见仪表盘后继续计时。
- 主题、亮度、音量和自动换肤配置通过带版本、长度与 CRC 校验的 NVS 记录持久化，并采用延迟写入降低 Flash 写放大；自动轮换本身不会反复写入首选主题。
- 为后续主题化开机动画和任务完成页预留独立、强类型的 Startup / Completion 接口；当前继续使用系统默认场景。
- 补充主题架构、Cyberpunk/Nothing/游戏主题设计稿、实屏截图和视觉验收资料。

## v2.2.0

- 增加可降级的 Codex transcript 增量监听，在暂停或中断未触发 `Stop` hook 时按 `turn_id` 及时清除活动指示；为每个运行任务增加由 transcript 续租的 60 分钟 TTL，并强化多任务身份优先级和重复结束幂等处理。
- 增加 Codex 每日 Token 桶的 UTC/本地时间诊断日志；当服务端今日桶与本机累计的差异同时达到 100 万 Token 和 25% 时，今日用量优先使用本机值，并同步替换近7天合计中的当天部分。

## v2.1.0

- 新增 Token 活动主页：当 Codex 只返回有效 7d 限额窗口时，自动显示今日/近7天 Token 消耗和 7d 额度；5h 窗口恢复时自动切回原有双余量布局。
- macOS daemon 新增可选 `account/usage/read` 读取、近 7 个自然日聚合和缓存回退；旧版 Codex 或接口失败不会阻断限额刷新。
- 修复服务端当天 Token 桶尚未生成时误显示 `0`：改用本机 session `token_count` 增量作为实时回退，并过滤 fork 的历史事件回放。
- `usage` BLE payload 以兼容方式新增 `td` / `t7` 字段，并增加宿主与串口 Token 布局预览命令。
- 完善 Token 主页视觉样式：统一标题和数字字体、字号与间距，移除多余滚动条和标点，并使用可配置的 Montserrat TTF 渲染 Token 数值。

## v2.0.2

- 修复任务完成摘要中 `“”‘’—–…·` 等常见智能标点无法渲染的问题：这些符号现在会进入内置 UI 字体，作为 TinyTTF 字体缺字时的 fallback。
- 修复 macOS CoreBluetooth 残留连接下设备停止广播后，多设备 discovery 无法重新接管连接的问题；补充发现来源与扫描健康状态。
- 为 BLE 连接和断开清理增加独立超时，避免 CoreBluetooth 卡住单台设备 worker 并阻止后续重连。
- 完善公开仓库文档、快速开始、项目截图、安全说明和 Python 包元数据，并以 MIT License 开源。
- README 增加 CodexMeter 实物运行照片。

## v2.0.1

- 任务完成摘要改为固定四行正文区域，并把宿主摘要上限提高到 64 字符。
- BLE 告警在收到 ACK 前保持为 in-flight，断连后重试，并由固件按告警 ID 去重。
- BLE 连接后校验完整芯片 identity，并用原子写入持久化注册表升级。
- daemon 增加单实例锁、关键任务 fail-fast 监督和约 100MB 的日志总上限。
- 自动烧录仅选择通过 identity 校验的 CodexMeter 串口，恢复模式需要显式 `--force`。
- 对齐宿主与固件的 512 字节 payload 边界，并锁定已验证的依赖版本。

## v2.0.0

- 新增多 CodexMeter 支持：daemon 可同时驱动多台已登记设备，每台设备拥有独立 BLE 队列、ACK、健康状态和重连 backoff。
- 新增设备登记与发现命令：`codexmeterctl devices scan/list/adopt/rename/enable/disable`。
- 固件新增稳定设备身份，BLE 广播名改为 `CodexMeter-<short_id>`，并提供 BLE/USB identity 查询。
- USB 日志和截图工具支持 `--device` 与 `--list`，多设备连接时避免误选设备。
- `flash-mac.sh` 支持多串口保护和 `--all` 批量烧录。

- 新增 AMOLED 防烧屏像素漂移：正常页每 10 分钟在 2px 范围内整体轻微移动，任务完成提醒和亮度浮层保持固定。
- 强化 macOS 端 BLE 健康检查：TX ACK notify 订阅失败时不再降级为“写入成功”，并增加 45 秒应用层 heartbeat，避免 CoreBluetooth 假活导致设备端持续 `stale`。
- Codex 用量读取失败时，会用上一次成功数据发送 `stale` heartbeat，保持 BLE 链路探活并明确区分数据源异常和蓝牙异常。
- `codexmeterctl status` 会返回 BLE 健康字段，包括连接状态、队列深度、最近写入/ACK 时间和连续失败次数。
- 增加 Codex 用量快照稳定器：过滤 App Server 偶发返回的 7d 空窗口，避免屏幕短暂显示 `100%` 和 `7d 后重置`。

## v0.5.1

- 修复 macOS CoreBluetooth 连接假活时，daemon 持续排队但设备端用量变为 `stale` 的问题：BLE 写入或设备 ACK 超时后会主动断开并自动重连。
- 新增 `codexmeterd --ble-write-timeout` 和 `--ble-ack-timeout` 参数，用于调整 BLE 自动恢复超时。

## v0.5.0

- 新增 QMI8658 IMU 屏幕方向自适应：支持 0/90/180/270 度自动旋转，并在方向切换时压暗重绘。
- 新增 `imu`、`rotate auto`、`rotate 0/90/180/270` 串口调试命令，方便查看 IMU 读数和手动锁定方向。
- USB 截图会按当前物理方向输出，便于自动旋转场景下做视觉 QA。

## v0.4.0

- 新增中间按键切换 AMOLED 亮屏/关屏能力，并提供 `screen_on`、`screen_off`、`screen_toggle` 串口调试命令。
- 新增自动亮屏/关屏策略：Mac 锁屏 5 分钟或 BLE 断连 5 分钟后关屏，Mac 解锁或未锁屏时 BLE 恢复后亮屏。
- 新增 `control` BLE payload 和 `codexmeterctl screen-on/screen-off` 调试命令。
- 新增左/右按键亮度调节：左键降低亮度，右键增加亮度，调整后显示 3 秒亮度进度条。

## v0.3.0

- 新增 ESP32 设备端环形日志和 `./logs.sh` 查询工具，支持日志查询、清空和实时跟随。
- 修复连续 BLE 写入时固件单槽 RX 缓冲被覆盖，导致完成提醒出现但小蓝点偶发不消失的问题。

## v0.2.0

- 新增运行中任务指示：Codex 任务开始后在正常页底部显示对应数量的小蓝点，任务完成后自动隐藏。
- 新增 `activity` BLE payload 和 `codexmeterctl demo-activity` 调试命令。
- 安装脚本会同时安装 `UserPromptSubmit` 与 `Stop` hook，用于跟踪任务开始和结束。
- 修复 start/stop hook 输入 ID 不一致时，任务完成后小蓝点可能残留的问题。
- Stop hook 改为发送单个 `task_complete` 事件，避免完成提醒已显示但运行中任务数未清零。
- 新增 USB 串口截图工具：固件支持 `screenshot` 命令，`./screenshot.sh` 可抓取当前屏幕并保存为 PNG，用于视觉 QA。

## v0.1.0

首个可用里程碑版本。

- macOS daemon：通过 Codex App Server 读取 `codex` 订阅 5h/7d 剩余用量。
- BLE：向设备发送 `usage` 和 `alert` payload，payload 限制在 512 bytes 内。
- Codex 完成提醒：安装 `Stop` hook，任务完成后显示提醒摘要。
- ESP32 固件：支持 Waveshare ESP32-S3-Touch-AMOLED-2.16。
- UI：显示 Codex 标识、剩余用量、电池状态、5h/7d 剩余百分比和重置倒计时。
- 重置倒计时：5h 显示为 `HH:MM 后重置`，7d 显示为 `xd 后重置`。
- 提醒页：红、黄、绿全屏闪动后显示“任务完成！”和任务摘要。
- 字体与文本：加入中文子集字体和设备字库清洗，避免摘要乱码。
- 开发体验：提供安装脚本、刷机脚本、串口 demo 命令和 Python 测试。
